type Profile @model @auth(rules: [{allow: public}]) {
  id: ID!
  username: String
  email: String
  profPic: String
}

type Pref @model @auth(rules: [{allow: public}]) {
  id: ID!
  type: String
  name: String
  priority: String
} 